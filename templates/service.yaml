apiVersion: v1
kind: Service
metadata:
  name: {{ include "vce-validator.fullname" . }}
  labels:
    {{- include "vce-validator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vce-validator.selectorLabels" . | nindent 4 }}
