apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vce-validator.fullname" . }}-test-connection"
  labels:
    {{- include "vce-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vce-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
